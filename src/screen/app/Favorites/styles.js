import React from 'react';

import { styles } from './styles';
import { SafeAreaView, ScrollView } from 'react-native';

const Favorites = () => {
    return (
        <SafeAreaView>
            <ScrollView>
                <Text>

                </Text>
            </ScrollView>
        </SafeAreaView>

    )
}